export default function csfPlugin(svelteOptions: any): {
    name: string;
    enforce: string;
    transform(code: any, id: any): Promise<{
        code: string;
        map: import("magic-string").SourceMap;
    } | undefined>;
};
