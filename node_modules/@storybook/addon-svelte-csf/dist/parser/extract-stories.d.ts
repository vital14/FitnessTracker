interface StoryDef {
    storyId: string;
    name: string;
    template: boolean;
    source: string;
    hasArgs: boolean;
}
interface MetaDef {
    title?: string;
    id?: string;
}
interface StoriesDef {
    meta: MetaDef;
    stories: Record<string, StoryDef>;
    allocatedIds: string[];
}
/**
 * Parse a Svelte component and extract stories.
 * @param component Component Source
 * @returns Map of storyName -> source
 */
export declare function extractStories(component: string): StoriesDef;
export {};
