/**
 * Generate one test file per component so that Jest can
 * run them in parallel.
 */
export declare const transformPlaywrightJson: (index: Record<string, any>) => {
    [key: string]: string;
};
